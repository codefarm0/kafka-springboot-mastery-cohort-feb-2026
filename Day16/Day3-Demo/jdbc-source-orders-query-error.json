{
  "name": "jdbc-source-orders-query-error",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSourceConnector",
    "tasks.max": "1",
    "connection.url": "jdbc:postgresql://postgres:5432/ecommerce_db",
    "connection.user": "kafka_user",
    "connection.password": "kafka_password",
    "mode": "incrementing",
    "incrementing.column.name": "order_id1",
    "query": "SELECT * FROM (SELECT order_id, customer_id, product_id, quantity, price, order_status, created_at, updated_at FROM orders WHERE order_status = 'PENDING') AS filtered_orders",
    "topic.prefix": "db-query1",
    "poll.interval.ms": "5000"
  }
}